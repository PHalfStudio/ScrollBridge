# CoreTests

核心测试由 Xcode target（目标）MouseBridgeTests 执行，当前 135 项，覆盖关于页元信息、关于页构建号展示、关于页 toolbar（工具栏）滚动避让、关于页隐私说明入口、关于页真实 AppIcon（应用图标）展示、关于页参考项目致谢、隐私说明字符输入序列边界、项目构建设置权限隐私文案、基础 Info.plist（信息属性表）权限隐私文案、README 卸载说明、README 厂商按钮限制说明、排除 App 输入框、选择 App 添加入口、添加按钮与列表行 VoiceOver（屏幕朗读）提示、配置默认值、滚动方向、CGEvent（事件对象）处理链防递归、横向滚轮非平滑保护、混合轴滚动保留、平滑滚动、平滑滚动页持续时间本地化格式、设置开关、选择器、文本输入框与恢复默认设置按钮 VoiceOver（屏幕朗读）提示、快捷键录制 VoiceOver（屏幕朗读）提示、macOS（苹果桌面系统）预制快捷键选择、文本输入时暂停快捷键录制、平滑滚动页滑块与选择器可访问性标签与提示、平滑持续时间、平滑曲线、惯性模拟、平滑滚动队列合并、按键映射、新增按键映射需先选择鼠标按钮、鼠标按钮选择前暂不录制快捷键、选中鼠标按钮后自动开启快捷键录制、左键/右键映射保护、无效 keyCode（键码）配置清理保护、映射范围默认值、映射列表摘要、按键映射列表行、按键映射开关、列表操作按钮与编辑按钮 VoiceOver（屏幕朗读）提示、鼠标按钮录制、快捷键录制、录制超时、快捷键注入顺序、事件监听恢复、事件回调性能统计、诊断错误码导出、权限状态、权限重置说明、权限重启状态推断、权限状态操作提示、设置侧边栏列表行、设置选择器、NSStatusItem（系统状态栏项）点击入口、菜单栏控件、欢迎引导按钮、权限授权按钮和诊断操作按钮 VoiceOver（屏幕朗读）提示、设备标识、设备刷新按钮、设备行与设备类型选择器 VoiceOver（屏幕朗读）提示、诊断页设备清单、诊断行 VoiceOver（屏幕朗读）提示、诊断回调耗时本地化格式、设备分类、设备诊断字段摘要、冲突检测、配置迁移、损坏配置备份、配置读取失败诊断错误码和配置持久化，以及 AppIcon（应用图标）资产完整性、诊断日志本地化键、事件监听异常本地化键、最后输入事件摘要本地化、最近错误显示策略、事件监听启动错误本地化键、默认按键映射备注本地化约束、菜单栏显示设置文案、权限弹窗 InfoPlist.strings（信息属性表字符串）本地化、菜单栏显示设置生效策略、菜单栏隐藏后的临时恢复入口和事件监听快捷键录制事件范围、事件监听不订阅全局键盘事件、滚动事件摘要节流、权限重启生效状态、中键映射风险提示、已有按键映射编辑保护和重复按键映射冲突提示、快捷键录制取消生命周期和按键映射名称迁移、Magic Mouse（苹果妙控鼠标）处理策略、核心功能未生效原因提示、低置信度鼠标候选保护、设备页低置信度分类提示、缺权限事件监听状态提示、退出前事件监听停止处理和总开关关闭时事件监听状态提示。
