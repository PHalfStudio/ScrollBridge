# Status

## 已完成

- 菜单栏、NSStatusItem（系统状态栏项）点击入口、设置窗口、设置侧边栏列表行、首次启动、权限与诊断、包含真实 AppIcon（应用图标）、版本、构建号、外部开源许可链接、外部隐私说明链接与参考项目致谢的关于页已实现；权限弹窗和项目构建设置文案已说明本机处理、不上传、不保存键入文本、不保存录制过程中的字符输入序列、不读取剪贴板，权限页会显示授权、重启和未知状态的操作提示，菜单栏控件、欢迎引导按钮、权限授权按钮和诊断操作按钮提供 VoiceOver（屏幕朗读）说明，并在用户走过授权入口后推断需要重启状态。
- 滚轮方向、平滑滚动、平滑滚动页持续时间本地化格式、平滑滚动页滑块与选择器可访问性标签与提示、平滑持续时间、平滑曲线、惯性模拟、侧键映射、映射范围与备注展示、按键映射圆角列表、按键映射列表行、按键映射开关、列表操作按钮与编辑按钮 VoiceOver（屏幕朗读）提示、设置开关、选择器、文本输入框与恢复默认设置按钮 VoiceOver（屏幕朗读）提示、快捷键录制 VoiceOver（屏幕朗读）提示、macOS（苹果桌面系统）预制键盘快捷键与系统动作分组选择、显示桌面系统动作、独立按键拼接录制、文本输入时暂停快捷键录制、录制视图只在激活时获取焦点、鼠标按钮松开后发送快捷键、系统快捷键 HID（硬件输入设备）发送、新增按键映射需先选择鼠标按钮、鼠标按钮选择前暂不录制快捷键、选中鼠标按钮后自动开启快捷键录制、鼠标按钮录制、快捷键录制取消/清空/15 秒超时、无效 keyCode（键码）配置清理、排除 App 输入框、选择 App 添加入口、添加按钮、删除按钮与列表行 VoiceOver（屏幕朗读）提示、设备手动分类、设备诊断字段展示、设备刷新按钮、设备行与设备类型选择器 VoiceOver（屏幕朗读）提示、诊断页设备清单、诊断行 VoiceOver（屏幕朗读）提示、开机自启、多语言已实现。
- 睡眠唤醒恢复、事件监听延迟重试与连续异常停止提示、常见鼠标工具冲突提示、带错误码、性能统计和本地化回调耗时的诊断导出已实现，设备分类测试、低置信度鼠标候选保护、设备页低置信度分类提示、缺权限事件监听状态提示、退出前事件监听停止处理、总开关关闭时事件监听状态提示、README 卸载说明、README 厂商按钮限制说明、README 开源项目结构、README_EN.md（英文说明文档）、Release（发布页）下载链接、linux.do 标识与社区说明、build/（构建目录）忽略规则、关于页外部链接、GitHub Release（发布页）更新检查、关于页手动检查更新按钮、当前是否为最新版本标识、GitHub 发布页链接、每日自动检查和 7 天内不提醒策略、事件监听不订阅全局键盘事件、滚动事件摘要节流、配置迁移、损坏配置备份和配置读取失败诊断错误码已实现。
- 已生成 build/ScrollBridge.app 和 build/ScrollBridge.dmg。

## 已验证

- build-for-testing（测试构建）通过，包含 MouseBridgeUITests UI 测试 target（界面测试目标）。
- Core unit tests（核心单元测试）：150 项通过。
- Release build（发布构建）通过，包含 arm64（苹果芯片）和 x86_64（英特尔）。
- codesign verify（签名校验）通过。
- hdiutil verify（磁盘映像校验）通过。
- 未发现 .env（环境变量文件）、密钥文件或明显凭据文本。

## 未完成外部项

- 当前钥匙串只有 Apple Development（苹果开发证书），没有 Developer ID Application（开发者 ID 应用证书），所以 Notarization（苹果公证）未执行。
- UI automation（界面自动化）需要本机自动化授权；当前 UI 测试 target（目标）可构建。2026-06-19 尝试运行 MouseBridgeUITests（界面测试目标），约 149 秒后停在测试启动与日志归集阶段，结果为 TEST INTERRUPTED（测试被中断），未标记为通过。
- 真实鼠标设备矩阵、Intel Mac（英特尔电脑）实机、24 小时稳定性测试未在当前环境完成。
